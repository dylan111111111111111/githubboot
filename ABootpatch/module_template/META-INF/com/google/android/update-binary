#!/sbin/sh
SKIPUNZIP=1; OUTFD=$2; ZIPFILE=$3; MODDIR=/data/adb/modules/my_module
ui_print() { echo "ui_print $1" >> /proc/self/fd/$OUTFD; echo "ui_print" >> /proc/self/fd/$OUTFD; }
ui_print "Installing module..."; mkdir -p "$MODDIR"; unzip -o "$ZIPFILE" -d "$MODDIR"; ui_print "Done!"; exit 0
